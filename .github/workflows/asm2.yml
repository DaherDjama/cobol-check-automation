
name: ASM2 Challenge Automation

on:
  workflow_dispatch:

jobs:
  asm2-fix:
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH tools
        run: sudo apt-get install -y sshpass

      - name: Create Python script locally
        run: |
          cat > fix_asm.py << 'PYEOF'
          from zoautil_py import datasets
          import re
          sq = chr(39)
          
          # --- PART 1: Modify Source Code ---
          s = datasets.read("ZXP.PUBLIC.SOURCE(ASMPGM)")
          
          # Replace ALL occurrences of register 2 with register 6
          # Use regex to match "2," or " 2," patterns in instruction operands
          s = re.sub(r'\b2,', '6,', s)
          
          # Replace ALL occurrences of register 3 with register 7
          s = re.sub(r'\b3,', '7,', s)
          
          # Now fix the specific values
          s = s.replace("=F" + sq + "4" + sq, "=F" + sq + "10" + sq)
          s = s.replace("=F" + sq + "1" + sq, "=F" + sq + "0" + sq)  # Initialize sum to 0
          s = s.replace("=F" + sq + "1" + sq, "=F" + sq + "5" + sq)  # Add 5 each loop
          
          # Wait, the above won't work correctly. Let me be more specific:
          # Reset and do it properly
          s = datasets.read("ZXP.PUBLIC.SOURCE(ASMPGM)")
          
          # Specific replacements in correct order
          s = s.replace("SR 2,2", "SR 6,6")  # Clear register 6 (loop counter)
          s = s.replace("L 2,=C", "L 6,=C")
          s = s.replace("LA 2,=C", "LA 6,=C")
          s = s.replace("L 2,=F" + sq + "4" + sq, "L 6,=F" + sq + "10" + sq)
          s = s.replace("L 3,=F" + sq + "1" + sq, "L 7,=F" + sq + "0" + sq)
          s = s.replace("A 3,=F" + sq + "1" + sq, "A 7,=F" + sq + "5" + sq)
          s = s.replace("BCT 2,LOOP", "BCT 6,LOOP")
          s = s.replace("LH 3,HALFCON", "LH 7,HALFCON")
          s = s.replace("A 3,FULLCON", "A 7,FULLCON")
          s = s.replace("ST 3,HEXCON", "ST 7,HEXCON")
          
          datasets.write("Z89674.SOURCE(ASM2PGM)", s)
          print("Source code updated successfully")
          
          # --- PART 2: Modify JCL (Fixing line length issue) ---
          j = datasets.read("ZXP.PUBLIC.JCL(ASMPGM)")
          
          lines = j.split('\n')
          new_lines = []
          for line in lines:
              new_line = line.replace("ASMPGM", "ASM2PGM")
              if len(new_line) > 80:
                  new_line = new_line[:80]
              new_lines.append(new_line)
          
          j_fixed = '\n'.join(new_lines)
          
          datasets.write("Z89674.JCL(ASM2PGM)", j_fixed)
          print("JCL updated successfully")
          PYEOF

      - name: Copy script to remote and execute
        run: |
          sshpass -p "${{ secrets.SSH_PASSWORD }}" scp -o StrictHostKeyChecking=no fix_asm.py ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}:/z/z89674/fix_asm.py
          
          sshpass -p "${{ secrets.SSH_PASSWORD }}" ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} "source /etc/profile 2>/dev/null || true; python3 /z/z89674/fix_asm.py"
          
          sshpass -p "${{ secrets.SSH_PASSWORD }}" ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} "submit //'Z89674.JCL(ASM2PGM)'"
          
          sleep 30
          
          sshpass -p "${{ secrets.SSH_PASSWORD }}" ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} "submit //'ZXP.PUBLIC.JCL(CHKASM2)'"
          echo 'Validation job submitted!'
